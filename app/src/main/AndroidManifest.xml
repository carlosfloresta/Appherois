<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.etecia.appherois">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HeroisActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CapitaoAmericaActivity" />
        <activity android:name=".HomemDeFerroActivity" />
        <activity android:name=".HomemAranhaActivity" />
        <activity android:name=".MagnetoActivity" />
        <activity android:name=".HulkActivity" />
        <activity android:name=".DeadpoolActivity" />
        <activity android:name=".LoganActivity" />
        <activity android:name=".MotoqueiroFantasmaActivity" />
        <activity android:name=".MaquinaDeCombateActivity" />
        <activity android:name=".SupermanActivity" />
        <activity android:name=".BatmanActivity" />
        <activity android:name=".DemolidorActivity" />
        <activity android:name=".HellboyActivity" />
        <activity android:name=".ThorActivity" />
        <activity android:name=".ColossusActivity" />
        <activity android:name=".SurfistaPrateadoActivity"></activity>
    </application>

</manifest>